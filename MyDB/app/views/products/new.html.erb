<% if @product.errors.any? %> 
 <div id="error_explanation">  
    <h2><%= pluralize(@product.errors.size, "error") %> 
  prohibited this object from being saved:</h2> 
     <ul> 
         <% @product.errors.full_messages.each do |message| %>    
    
        <li><%= message %></li>   
       <% end %>  
      </ul> 
     </div><% end %>
<% content_for :title, "New Product" %>

<div style="
  max-width: 500px;
  margin: 40px auto;
  padding: 24px;
  background: #ffffff;
  border-radius: 10px;
  box-shadow: 0 8px 24px rgba(0,0,0,0.10);
  font-family: Arial, sans-serif;
">

  <h2>New Product</h2>

  <%= render "form", product: @product %>

</div>
